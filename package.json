{
  "name": "footprint-metric-adapters",
  "version": "0.1.0",
  "description": "Metric collection adapters for FootPrint — window strategies, backend adapters, and LLM-navigable Tree of IDs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sanjay1909/footprint-metric-adapters.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "rm -rf dist coverage node_modules package-lock.json",
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest --coverage",
    "test:watch": "jest --passWithNoTests --watchAll --coverage=false"
  },
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist/**/*",
    "!dist/test",
    "!dist/types/test"
  ],
  "dependencies": {
    "footprint": "file:../footPrint-fresh"
  },
  "devDependencies": {
    "typescript": "~5.4.5",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.2"
  }
}
